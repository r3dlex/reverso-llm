---
title: "OLLAMA-RP-G5: Cloud routing alias uses colon (:cloud) not hyphen (-cloud)"
status: complete
state: complete
category: bugfix
slug: ollama-reverso-provider-g5-cloud-suffix-colon
owner: unassigned
execution_type: HITL
---

# OLLAMA-RP-G5: Cloud routing alias uses colon (:cloud) not hyphen (-cloud)

> *This was generated by AI during triage.*

## Traceability

- BRD: `docs/01-brd.md`
- PRD: `docs/specifications/ACTIVE/ollama-reverso-provider.md`
- ADR: `docs/architecture/adr/0019-ollama-cloud-model-authority.md`
- Parent: `.ai/work-intake/ollama-reverso-provider.md`
- Version impact: bugfix to G4 (PR #120) routing-constant and ADR-0019 wording

## What to build

Correct `CLOUD_ROUTING_SUFFIX` in `src/reverso/protocols/adapters/ollama/auth.py` from
`"-cloud"` (hyphen) to `":cloud"` (colon). Update the matching ADR-0019 wording so the
documented routing alias matches the actual Ollama 0.32.14 behavior. Add one unit
test asserting the new constant and the catalog's authority-routing mapping. Update
existing test fixtures that hardcoded the wrong hyphen form to use the colon form.

## Acceptance criteria

- [x] `CLOUD_ROUTING_SUFFIX` equals `":cloud"`.
- [x] `_authority_routing_ids(...)` returns the colon-suffixed alias for an authority bare id.
- [x] Test fixtures using `*-cloud` switch to `*:cloud` for the corrected Ollama 0.32.14 behavior.
- [x] ADR-0019 wording matches the corrected routing alias.
- [x] `pytest tests/unit/test_ollama_adapter.py -q` is green.

## Evidence

- Red command: `pytest tests/unit/test_ollama_adapter.py::test_cloud_routing_suffix_uses_colon_form -q`
- Green command: same, after `auth.py` constant flip
- Recorded in `.ai/evidence/OLLAMA-RP-G5.json`
