name: AI SDLC Pre-commit Checks

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  _prek-check-self-hosted:
    name: "Pre-commit Hooks (self-hosted)"
    runs-on: [self-hosted, ubuntu-latest]
    timeout-minutes: 30
    continue-on-error: true
    outputs:
      passed: ${{ steps.mark.outputs.passed }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      # uv ships its own Python toolchain; the runner's cached
      # tool-cache Python is broken (missing libpython shared object),
      # so nothing here may rely on bare python/pip from PATH.
      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - name: Run prek
        run: uvx prek run --all-files
      - name: Validate .rules.ts
        run: bash scripts/validate-rules.sh
      - name: Archgate structural check
        run: bash scripts/archgate.sh structural .rules.ts
      - id: mark
        if: success()
        run: echo "passed=true" >> "$GITHUB_OUTPUT"

  prek-check:
    name: Pre-commit Hooks
    needs: _prek-check-self-hosted
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Route
        run: echo "self-hosted passed=${{ needs._prek-check-self-hosted.outputs.passed }}"
      - name: Checkout
        if: needs._prek-check-self-hosted.outputs.passed != 'true'
        uses: actions/checkout@v4
      - name: Install uv
        if: needs._prek-check-self-hosted.outputs.passed != 'true'
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"
      - name: Run prek
        if: needs._prek-check-self-hosted.outputs.passed != 'true'
        run: uvx prek run --all-files
      - name: Validate .rules.ts
        if: needs._prek-check-self-hosted.outputs.passed != 'true'
        run: bash scripts/validate-rules.sh
      - name: Archgate structural check
        if: needs._prek-check-self-hosted.outputs.passed != 'true'
        run: bash scripts/archgate.sh structural .rules.ts
