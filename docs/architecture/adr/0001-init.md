# ADR 0001: Init (v3)

## Status
Accepted.

## Context
`br4vesirrobin/reverso` adopts the v3 AI-SDLC scaffold. Stack signal: Python.

## Decision
Adopt `.ai/`, `.memory/`, and `docs/` layout from `r3dlex/skills/ai-sdlc-init/modules/documentation-blueprint.md`.

## Consequences
`topology_type=standalone`, `sync_strategy=physical-copy`. `.memory/human-override/` is terminal priority.
