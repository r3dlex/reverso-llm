[project]
name = "reverso"
version = "0.1.0"
description = "Local LLM gateway wrapping Claude Code and Codex CLIs with session persistence"
requires-python = ">=3.11"
dependencies = [
    "litellm[proxy]>=1.86.2",
    "httpx",
    "fastapi",
    "pyyaml",
    "uvicorn",
    "websockets",
    "psutil",
    "headroom-ai[all]==0.32.1",
]

[project.scripts]
reverso-proxy = "reverso.proxy.main:main"
reverso-daemon = "reverso.daemon.main:main"
reverso-codex-sync = "reverso.codex_sync:main"
reverso-claude-code-sync = "reverso.claude_code_sync:main"
reverso-client-sync = "reverso.client_sync:main"
reverso-catalog-refresh = "reverso.catalog_refresh:main"

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "ruff==0.6.0"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "integration: marks tests as integration tests (deselect with -m 'not integration')",
]

[tool.ruff.lint.per-file-ignores]
"src/reverso/proxy/app.py" = ["E402"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/reverso"]

[tool.hatch.metadata]
allow-direct-references = true
